1 - Default Config for Database is inside the docker-compose.yml file
2 - Run the following commands to start and configure the server

```
$ docker-compose up -d
$ npm install
$ ./node_modules/.bin/gulp
```
